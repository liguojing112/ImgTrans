# TASK-M4-006：发布加固、秘密扫描、版本与回滚

**状态**：前置发布加固完成，待 M4-005 与跨平台 CI 收口（2026-07-18）
**依赖**：TASK-M4-005

## 目标

完成第一阶段发布前的安全、版本、回滚、已知限制和全量验收收口。

## 范围与验收

- 扫描源码、安装产物、日志、崩溃信息和模型清单中的密钥与令牌。
- 固化版本号、兼容性、升级/回滚包和模型独立回滚流程。
- 执行全量自动测试与 Windows/macOS 发布清单，无 P0 缺陷。
- 明确记录艺术字、复杂纹理修复和真实素材质量的已知限制与人工降级路径。

## 预计修改文件

- `src/`、`server/`、`scripts/`、`tests/release/`、现有发布与验收文档

## 测试命令

```powershell
python -m pytest -q
python -m compileall -q src server tests
python -m src --smoke-test
python -m server --smoke-test
```

## 已实施

- 校验 `pyproject.toml`、桌面包和服务端三处语义版本完全一致。
- 扫描正式源码与安装产物中的硬编码密钥、Bearer 令牌、数据库凭据、AWS 访问键、私钥、敏感文件和本机工作区路径；只报告类别和相对路径，不回显秘密。
- Windows/macOS 归档后生成确定性发布清单，绑定版本、平台、最低系统、架构、文件大小和 SHA-256。
- 发布清单明确模型不进入安装包、模型独立更新/回滚、桌面回滚使用上一份已验证归档且保留当前活动模型。
- 两个现有发布 CI Job 均在测试前执行源码门禁，并把 ZIP 与对应清单一起上传；未新增 Artifact 类型或额外人工下载要求。
- Windows 本地完整回归为 `365 passed`；桌面/服务端烟测、编译和差异检查通过。

## 已知限制与降级

- 艺术字、弧形文字和密集旋转词云不承诺全自动高度复刻，使用自动初稿、手动框选、字体/样式/路径编辑后导出。
- 复杂纹理、商品边缘和主体附近修复可能失败，使用人工蒙版调整、撤销、重试或保留原图。
- 真实 Apple Silicon GUI、Keychain、正式开发者签名、公证、安装、升级和卸载仍属于 M4-005，当前不得标记为完成。
- M4-006 最终关闭还需要现有 Windows/macOS CI 通过本次加固门禁，并完成或正式豁免 M4-005。
