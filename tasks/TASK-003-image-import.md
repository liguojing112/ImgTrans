# TASK-003：M1 单图导入与导出

**状态**：待开始
**里程碑**：M1 单图翻译闭环
**优先级**：P0
**依赖**：TASK-002

## 关联需求

FR-IMG-002～009、FR-IMG-012。

## 目标

实现单图 JPG/JPEG、PNG、WebP 导入、EXIF 方向规范化、三级限制回退和 JPG/PNG/WebP/静态 GIF/单页 TIFF 安全导出。

## 范围

- 格式与内容校验、尺寸和解码安全限制。
- 远程配置接口、最近缓存与内置默认值选择。
- Alpha、工作色彩模式和 EXIF 方向处理。
- 临时文件写入与成功后原子落盘。
- 不实现批量列表、OCR、翻译或编辑器。

## 完成标准

- 输入/输出格式通过 `ACCEPTANCE_CRITERIA.md` 对应测试。
- 远程、缓存和内置限制故障路径可复现。
- 原图不会被覆盖，失败输出不会标记成功。
- 单元测试覆盖格式、边界、Alpha、方向和错误处理。
