# TASK-002：M1 正式项目骨架

**状态**：待开始
**里程碑**：M1 单图翻译闭环
**优先级**：P0
**依赖**：M0 技术决策收敛

## 目标

按 `docs/ARCHITECTURE.md` 建立正式桌面端、服务端契约和测试骨架。此任务不在 M0 原型阶段实施。

## 范围

- 正式依赖与锁定策略。
- `src/desktop` 分层入口和无业务空壳。
- `src/shared_contracts` 最小契约。
- 单元、集成和 UI 测试目录及统一测试命令。
- 配置、脱敏日志和平台路径基础设施。

## 完成标准

- 干净环境可复现安装。
- 正式模块可以导入并运行最小启动冒烟。
- 测试框架可执行且不依赖真实模型或第三方服务。
- 不把 `prototypes/` 直接复制为正式产品代码。
