# 候选框与目标区域混淆：管线式系统中的通用错误理论

这个错误在算法/软件工程层面的通用理论可以总结为：

### 🧠 核心错误：把"检索区域"误当成了"目标区域"
这是一个经典的**"候选框（Proposal）与目标（Target）混淆"**问题。
- **检索区域（检测框）**：表示"目标大概率在这里面"，但它本身只是坐标建议，包含大量干扰背景。
- **目标区域（实例蒙版/笔画）**：表示"精确到像素的、真正要处理的东西"。

当直接把检测框当作最终目标去喂给下游处理模块时，就把一个原本需要精细判断的**分类/分割问题**，退化成了一个粗糙的**裁剪/填充问题**。

### ⚙️ 关键原理：任务边界定义决定了下游能力上限
1. **下游模块的能力边界是固定的**：修复模型（如LAMA）能处理的最复杂任务，是"填补细小裂缝/遮挡物"。它的算法假设输入区域中**大部分是背景，只有一小部分是需要修正的异常**。
2. **错误的输入会迫使模型越界**：如果喂进去的是大面积空白/异常区域，就让模型被迫去执行"凭空生成大片复杂纹理"的任务。这远远超出了它设计时的能力边界。**你让一个擅长微创手术的医生去做大面积植皮，再好的医生也会出问题。**

### 🔬 技术债务的典型特征：传递性污染
这类错误最隐蔽的地方在于它的**传导路径**：

**上游粗糙输出（粗框） → 下游被迫执行越级任务（大面积重绘） → 下游效果看似符合逻辑（填满了），但输出指标异常（背景模糊/变形）。**

这个现象在系统设计中叫**"错误放大"**——上游一个细微的简化（把框当蒙版），经过下游的加工后，变成了一个极其明显的、不可接受的视觉缺陷。

### 🔗 归因谬误的心理学基础
当错误最终暴露时，开发者最容易产生**"近因效应"归因偏差**——因为最后触碰到这个模块的是下游修复模型，所以第一反应是"模型不够聪明"，而完全忽略了在它之前的上游环节已经递了一个错误的输入。

真正的修正路径永远是**"先修输入，再调输出"**，而不是倒过来。

### 📐 理论警示
在管道式（Pipeline）系统设计中，**任何"近似取用"（把建议当事实）的行为，都会导致下游模块在不适合其设计的任务空间中运行，结果产生不可预测且难以修正的系统性偏差。** 数据层面的精确性（Pixel-level Precision）是保证算法模块在安全区工作的第一前提。
